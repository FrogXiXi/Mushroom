# 开发过程

## 目的

这个文档用于持续记录项目关键修改、对应功能影响、共通注意事项，以及后续对话需要快速掌握的上下文。

## 更新规则

- 关键修改包括：新增功能、重要交互调整、核心逻辑变更、重要 bug 修复、资源组织调整、构建或发布约束变更。
- 每次完成关键修改后，都要追加一条记录，至少写清楚改了什么、影响了什么功能、涉及哪些文件、还有什么约束。
- 新增共通注意事项时，先更新本文档，再同步检查 .github 下的 instructions 或 skill 是否需要一起更新。
- 不记录密钥、账号、令牌等敏感信息。

## 项目概览

- 这是一个以静态资源和前端脚本为主的项目，当前结构以 index.html、css、js、assets 为核心。
- 项目目标包含 GitHub Pages 发布，因此默认以纯静态站点兼容为前提，不引入服务端依赖。
- docs 目录用于沉淀需求和素材文档；.github 目录用于 Copilot 协作配置，不参与页面运行时逻辑。

## 共通注意事项

- 默认不要引入会破坏 GitHub Pages 部署前提的服务端方案、运行时依赖或无关构建污染。
- 每次新开对话时，先阅读这个文档，再开始理解和修改项目。
- 后续如果你补充新的共通点，就把它们追加到这里，作为长期约束来源。

- 为避免一次性输出过长导致写入失败或响应截断：在创建或写入多个文件前，先整理并列出要创建的文件框架与写入步骤，按分批次（分块）写入并逐步校验每一步结果。
- 为避免处理超大图片或请求过大：在批量处理图片前，先把图片素材上传或迁移到可通过 Web 访问的位置（并尽量生成适当分辨率的副本），然后按小批量（一张或少量一组）下载并逐张处理与核对，记录每批次结果后再继续下一批。

## 记录模板

### 日期

- 背景：
- 关键变动：
- 影响功能：
- 涉及文件：
- 备注 / 后续：

## 变更记录

### 2026-04-04

- 背景：为后续多人机协作建立统一上下文入口，并约束每次关键修改后的记录方式。
- 关键变动：新增本文件；新增仓库级 skill，用于要求新任务先读取本文档；新增 workspace instructions，兜底要求每次新对话先建立上下文；约定关键修改后必须回写变更记录；补充 GitHub Pages 兼容这一项长期约束。
- 影响功能：当前不影响站点运行时功能，只影响后续开发协作流程与上下文管理方式。
- 涉及文件：开发过程.md、.github/skills/dev-process/SKILL.md、.github/copilot-instructions.md。
- 备注 / 后续：后续新增“共通注意事项”时，优先更新本文档，再同步 skill 或 instructions；外部 skills fronted-design 和 UI-UX-PRO-MAX 当前按你的决定先跳过，不在本次仓库中落地。
### 2026-04-04 (2) — 全站视觉优化

- 背景：使用仓库内 frontend-design skill 指导，对全站样式做一次全面的视觉品质提升，保持蜡笔绘本美学方向不变、不改动 JS 逻辑与 HTML 结构。
- 关键变动：
  - **main.css**：扩充 CSS 变量（新增渐变阴影、弹簧缓动、辅助色）；按钮增加微渐变与内光泽、弹簧动效；纸张底纹叠加 SVG 噪声颗粒纹理；新增统一 range 输入控件样式；模块切换增加微位移过渡；相册按钮增加 hover/active 反馈。
  - **animations.css**：新增 gentleFloat、doodleSway、popIn、softPulse、twinkle 关键帧及对应工具类；fadeInUp 位移加大（10→16px）使入场更有层次。
  - **modules.css**：封面背景改为多层径向/线性渐变；标题增加双层文字阴影与字距；按钮改为白玻璃风格在金色背景上更醒目；全局 doodle 添加轻晃动画；蛋糕选择卡片增加半透明背景与阴影层级；色素瓶弹簧弹出与选中环；Tab/模板按钮统一渐变激活态；轮播箭头加 backdrop-blur；装饰素材卡片 hover 放大+浮起；进度条渐变+尾部高光；截图/相册弹窗 backdrop-blur 与加深阴影；仪式暗幕过渡加缓加深；结尾页渐变背景与更大行高/字距；相册缩略图弹簧动效与阴影。
- 影响功能：纯样式修改，不影响任何交互逻辑、Canvas 绑定、状态持久化。
- 涉及文件：css/main.css、css/animations.css、css/modules.css。
- 备注 / 后续：建议在浏览器中全流程走一遍确认视觉效果；如需继续精调某个模块的配色或动效，可以单独告诉我。

### 2026-04-04 (3) — 许愿环节关灯效果修复

- 背景：许愿环节的关灯效果依赖 canvas 内部整块压暗，导致仪式区出现明显黑框，蜡烛与蛋糕处在不同光照层里，观感割裂。
- 关键变动：将仪式暗场主逻辑收敛到页面级遮罩层，提升遮罩层级到 canvas 之上；移除 canvas 内整块黑色蒙版，改为更柔和的烛光受光层与全屏暗场渐变叠加，让蛋糕和蜡烛共享同一套灯光氛围。
- 影响功能：优化模块7 许愿环节的关灯、点蜡烛与许愿阶段视觉表现，不改变点蜡烛、吹蜡烛或模块跳转逻辑。
- 涉及文件：js/modules/ceremony.js、css/modules.css。
- 备注 / 后续：建议在移动端和桌面端各走一遍许愿流程，观察不同数量蜡烛下的暗场强度是否还需要继续微调。

### 2026-04-04 (4) — 色素选择与蜡笔笔触调整

- 背景：色素瓶底部文案与瓶身颜色对不上；许愿页暗场仍有无来源的中心打光；用户希望把涂鸦进一步做成蜡笔质感，并预留灯光/音乐按钮入口。
- 关键变动：移除奶油制作阶段色素瓶底部文字，只保留瓶身选择；把许愿页关灯后的环境光改为仅由点亮蜡烛产生，不再保留蛋糕中心的固定打光；新增灯光、音乐两个按钮作为后续挑战入口；增强蜡笔笔触的颗粒和叠涂纹理，并在调色板中加入黑色。
- 影响功能：优化模块3 选色 UI、模块4/5 涂鸦质感与颜色选择、模块7 许愿环节控件和关灯后的光照表现，不改变既有流程跳转。
- 涉及文件：index.html、css/modules.css、js/config.js、js/modules/creamMaking.js、js/modules/ceremony.js、js/utils.js。
- 备注 / 后续：可继续补充灯光/音乐按钮的真实玩法逻辑；若后续想再偏粉笔或油画风，可以继续微调蜡笔笔触参数。

### 2026-04-04 (5) — 手机端色素瓶改为点击上色

- 背景：手机端色素瓶沿用 Pointer 拖拽流程，实际操作中不易拖动到碗内，影响模块3 奶油上色体验。
- 关键变动：将色素瓶交互拆分为桌面端拖拽、触摸设备点击两套模式；手机端点击色素瓶即可直接触发倒色动画；同步把上色提示文案改为按设备类型显示，并给色素瓶补充 `touch-action: manipulation`。
- 影响功能：优化模块3 在手机端的上色操作，不影响桌面端原有拖拽流程，也不改变后续打发和抹面逻辑。
- 涉及文件：css/modules.css、js/modules/creamMaking.js。
- 备注 / 后续：建议真机走一遍模块3，确认不同手机浏览器下点击反馈和横向滚动色素栏的手感是否还需要继续细调。

### 2026-04-04 (6) — 手机端蜡烛拖拽坐标修复

- 背景：模块5 装饰阶段在手机端拖放蜡烛时，释放落点的坐标换算错误，导致蜡烛跳到画布左上区域。
- 关键变动：补强公共画布坐标工具，对 `touchend` 优先读取 `changedTouches`；同时修正装饰模块在拖放结束时的落点换算，直接使用当前触点对象计算 Canvas 坐标。
- 影响功能：修复模块5 手机端拖放蜡烛和其他装饰素材时的位置偏移问题，不影响桌面端鼠标拖拽逻辑。
- 涉及文件：js/modules/decorate.js、js/utils.js。
- 备注 / 后续：建议在手机端再验证一次“从素材栏拖到蛋糕上”和“拖动已放置蜡烛重新定位”这两条路径，确认都不再偏移。

### 2026-04-04 (7) — 全流程加入重头制作按钮

- 背景：为了应对中途误操作或流程卡住的情况，需要在任意制作阶段快速放弃当前草稿并重新开始，同时保留已存入相册的成品。
- 关键变动：在全局顶部操作区新增“重头制作”按钮；点击后弹出确认，清空当前 `zzhappy-state` 对应的蛋糕制作进度并刷新回开头；按钮默认在封面隐藏，在进入制作流程后全程可用。
- 影响功能：用户可以从模块2 之后任意阶段放弃当前未完成蛋糕并重新开始；相册数据因存储在独立的 IndexedDB，不受影响。
- 涉及文件：index.html、css/main.css、js/app.js。
- 备注 / 后续：如果后续想做得更细，可以把确认弹窗替换成站内自定义弹层，并细分“仅重做当前蛋糕”和“连相册也一起清空”两种操作。

### 2026-04-04 (8) — 仪式暗场、奶油流程与裱花笔触重做

- 背景：现有关灯效果带有模糊滤镜，蜡烛不够清晰；奶油制作顺序与期望不一致，且进入模块时有明显分段加载感；蛋糕绘制笔触也需要从蜡笔改成更像奶油裱花的效果。
- 关键变动：移除许愿页的灯光/音乐占位按钮，去掉暗场模糊并加深整体黑度，同时加强烛光局部高光；将奶油流程改为“打鸡蛋 → 打发奶油 → 滴入色素 → 抹面”，并对奶油制作模块增加并行预载和加载态，减少首屏分块出现的卡顿感；把原来的蜡笔笔触改为带立体高光和挤压纹理的奶油裱花笔触，同步更新界面文案。
- 影响功能：优化模块3 奶油制作的节奏与首屏体验，优化模块5/截图/许愿页里裱花与成品的视觉风格，并收紧模块7 的暗场表现。
- 涉及文件：index.html、css/modules.css、js/modules/ceremony.js、js/modules/creamMaking.js、js/modules/creamPaint.js、js/modules/decorate.js、js/utils.js。
- 备注 / 后续：建议重点在移动端实测三段流程：进入奶油制作页的加载感、打发后选色再抹面的节奏、以及裱花笔触在浅色和深色下的立体感是否还需要继续微调。

### 2026-04-04 (9) — 手机端吹蜡烛识别增强

- 背景：模块7 在桌面端可通过麦克风吹灭蜡烛，但手机端由于默认降噪、自动增益和固定阈值过高，吹气信号经常无法达到原先的识别条件。
- 关键变动：将麦克风申请改为优先请求单声道且尽量关闭回声消除、降噪和自动增益；把原先仅依赖频谱平均值的固定阈值检测，改为结合频域能量、低频能量、时域峰值和 RMS 的动态基线检测；补充吹气检测循环与音频资源的统一清理逻辑。
- 影响功能：提升模块7 在手机浏览器上的吹气识别成功率，同时保留点击火焰手动熄灭的兜底路径；桌面端麦克风吹气逻辑继续可用。
- 涉及文件：js/modules/ceremony.js、开发过程.md。
- 备注 / 后续：建议优先在 iPhone Safari 和 Android Chrome 真机验证一次，关注首次授权后直接吹气、环境噪声较大时的误触发率，以及拒绝麦克风权限后的点击兜底提示是否清晰。

### 2026-04-05 — `dev-process` skill 规范化重构

- 背景：现有 `dev-process` skill 能工作，但结构仍偏简略；为了更贴近通用 skill 规范，并让触发场景、流程约束和输出契约更清晰，需要按 `skill-creator` 思路整理。
- 关键变动：重写 `.github/skills/dev-process/SKILL.md` 的 frontmatter 与正文结构，补充 Trigger Signals、Required Context、Workflow、Output Contract、Guardrails、Checklist 与示例 prompt；新增 `.github/skills/dev-process/evals/evals.json` 作为最小测试样例集。
- 影响功能：不影响站点运行时功能，只增强仓库内协作流程的一致性，并提高 `dev-process` skill 的可触发性与可维护性。
- 涉及文件：.github/skills/dev-process/SKILL.md、.github/skills/dev-process/evals/evals.json、开发过程.md。
- 备注 / 后续：如果后续继续强化 skill，可以再补充 references 文件，把项目约束摘要与技能测试说明拆出去，继续压缩主 `SKILL.md` 的体积。

### 2026-04-05 — 自动化审计与批量 bug 修复

- 背景：网站存在多处功能 bug 和视觉细节问题，需要系统性检测和修复。搭建了 Python 自动化审计脚本，对图片引用、HTML-JS ID 绑定、JS 质量、CSS 问题和素材完整性做全面扫描，然后根据检测结果逐个修复。
- 关键变动：
  - **index.html**：封面四个 `.doodle` 图片元素的 `src=""` 空引用改为直接写入正确 WebP 路径（`doodle_01/03/05/08`），添加 `loading="lazy"`，避免页面加载时产生无效 404 请求。
  - **js/modules/decorate.js**：
    1. 修复旋转装饰元素点击检测（`_hitTestElement`）——原先使用轴对齐包围盒，旋转后角落点击不到；改为将点击坐标逆旋转到元素本地坐标系后再判断。
    2. 修复双指缩放→单指拖拽的过渡中断——从 `move` 中检测 `touches.length === 1` 时平滑过渡为拖拽模式；`end` 改为接受 `event` 参数，检查 `event.touches` 仍有残余时不清除 pinch 状态。
  - **css/main.css**：全局 `::-webkit-scrollbar` 隐藏改为仅 `body::-webkit-scrollbar`，并为 `.album-modal` 恢复 4px 滚动条，使相册弹窗在照片较多时可看到滚动指示。
  - **scripts/audit.py**：新增自动化审计脚本，覆盖 6 大检查维度（图片引用、ID 绑定、JS 质量、CSS 分析、素材目录、模块流程）+ DOM 结构检查，输出 JSON 报告。
- 影响功能：优化装饰模块的移动端操作体验（旋转后可正确选中、双指到单指过渡不再中断）、修复封面加载时的 404 请求、改善相册弹窗滚动反馈。
- 涉及文件：index.html、js/modules/decorate.js、css/main.css、scripts/audit.py。
- 备注 / 后续：审计脚本可随时通过 `python scripts/audit.py` 重新运行；剩余 19 个报告项中，2 个 HIGH 是假阳性（`birthday_cake.png` 仅为下载文件名、`global-toast` 是动态创建），16 个 innerHTML 使用均为静态/可信数据无 XSS 风险。后续如需更深度的运行时检测（如 Playwright 自动化浏览器测试），可在此基础上扩展。

### 2026-05-30 — Playwright E2E 全流程自动化测试 & 前端视觉美化

- 背景：需要搭建运行时自动化测试，模拟用户全流程点击并在每个关键步骤截图；同时做一轮纯 CSS 的前端视觉美化。
- 关键变动：
  - **scripts/e2e_test.py**：新建全流程 E2E 自动化测试脚本（Playwright Sync API + Chromium headless），覆盖 cover → cake-select → cream-making → decorate → screenshot → ceremony → cake-cut → ending → album 共 9 个模块，Mobile (390×844) + Desktop (1280×800) 两个视口各 20 步，共输出 40 张截图。解决了多个 Playwright 死锁问题：`wait()` 函数从 JS `wait_for_function` 改为 Python `time.sleep`，避免浏览器 rAF 循环或图片加载阻塞 CDP 轮询；仪式模块吹蜡烛后显式 `cancelAnimationFrame`；蜡烛添加和奶油制作等长交互通过 JS `evaluate` 快进；Mobile 视口启用 `has_touch=True, is_mobile=True` 适配触摸模式。
  - **css/modules.css**：封面背景从纯色改为多层径向/线性暖金渐变；结尾页渐变色差拉大并叠加装饰光晕伪元素；仪式暗场加深至 0.72 并延长过渡至 1200ms 弹簧缓动；仪式提示文字增强 text-shadow；相册弹窗改为渐变背景 + 虚线手绘边框 + 分层阴影。
  - **css/main.css**：模块切换位移从 ±6px 增至 ±14px，过渡改为 600ms cubic-bezier 弹簧缓动。
- 影响功能：纯 CSS 样式优化不影响交互逻辑；E2E 测试脚本不影响运行时代码。
- 涉及文件：scripts/e2e_test.py、css/modules.css、css/main.css。
- 备注 / 后续：测试通过 `python scripts/e2e_test.py` 运行（需 `.venv` 中已安装 `playwright` 和 Chromium）；截图输出到 `scripts/screenshots/`，JSON 报告在 `scripts/e2e_report.json`。

### 2026-04-05 — 奶油流程停顿确认与仪式下一步按钮

- 背景：奶油制作阶段的色素瓶在打发完成后才突然出现，节奏偏跳；吹蜡烛结束后也会自动瞬移到切蛋糕，不利于用户感知流程切换。与此同时，背景 doodle 装饰和制作工具尺寸还需要继续调到更适合桌面端与手机端。
- 关键变动：让奶油模块的色素瓶在打蛋/打发阶段就常驻底部，并通过禁用态提示“先打发、再滴色”；打发完成后新增右下角“完成打发奶油，去滴入色素”按钮，滴色完成后新增“完成滴入色素，去抹面”按钮；吹蜡烛模块改为点亮全部蜡烛后显示“许完愿了，去吹蜡烛”，吹灭全部蜡烛后显示“完成吹蜡烛，去切蛋糕”，不再自动跳转；背景增加更多 doodle 素材位，并重新平衡手机端与桌面端的尺寸；同步放大手机端抹刀、手机端和桌面端搅拌器尺寸。
- 影响功能：优化模块3 奶油制作与模块7 许愿仪式的节奏停顿感，减少突然切换；提升全站背景装饰层次，以及制作工具在不同设备上的可见性与可操作性。
- 涉及文件：index.html、css/modules.css、js/modules/creamMaking.js、js/modules/ceremony.js、开发过程.md。
- 备注 / 后续：建议在手机端真机重点确认三个节点的手感：打发完成后按钮是否遮挡视线、滴色完成后的停顿是否自然、吹灭蜡烛后再切蛋糕的节奏是否还需要更长的留白。

### 2026-05-30 (2) — 合并打发与滴色界面 & 修复仪式按钮定位

- 背景：奶油制作模块打发完成后弹出"完成打发奶油，去滴入色素"按钮是多余的中间步骤，用户希望打发和滴色在同一个界面无缝衔接；另外仪式模块的"去吹蜡烛"和"去切蛋糕"按钮在页面上不可见，存在定位 bug。
- 关键变动：
  - **js/modules/creamMaking.js**：`_onWhipDone()` 不再设置 `step='whip-ready'` 并显示中间按钮，改为直接切换到 `step='color'`、显示色素瓶选择器并启用交互，toast 提示"奶油打发好了，选个颜色给奶油上色吧"。同步清理 `_setStageLayout()` 中已不存在的 `whip-ready` 分支。
  - **css/modules.css**：移除 `.ceremony-actions` 上的 `position: relative`（该属性导致内部 `.btn-next` 的 `position: absolute; bottom: 5vh; right: 5vw` 相对于一个高度塌缩的 flex 容器定位，按钮跑到不可见区域）；同步移除无效的 `z-index: 24`（没有 position 时 z-index 不生效）。
- 影响功能：模块3 奶油制作流程简化为"打蛋 → 打发 → 选色（直接衔接）→ 滴色动画 → 抹面"；模块7 仪式阶段的"许完愿了，去吹蜡烛"和"完成吹蜡烛，去切蛋糕"按钮恢复正常定位，桌面端和移动端均可点击。
- 涉及文件：js/modules/creamMaking.js、css/modules.css。
- 备注 / 后续：已通过 Playwright 桌面端截图验证两项修复均生效；代码审查确认无残留引用、无内存泄漏、无竞态风险。

### 2026-04-08 — 许愿提示居中文案 & 打发进度条布局防抖

- 背景：许愿阶段的说明文字"默念愿望后，点右下角进入吹蜡烛"没有居中且文案偏生硬；奶油制作模块打鸡蛋后进入打发阶段时进度条出现导致整体 UI 上移，打发完毕进度条消失又导致 UI 下移，反复抖动影响观感。
- 关键变动：
  - **js/modules/ceremony.js**：`_onAllCandlesLit()` 的提示文案改为"闭上眼，默默许下你的心愿吧"，同时为 `.ceremony-hint` 添加 `ceremony-hint--center` 类使其固定居中显示；进入吹灭阶段时移除该类恢复普通定位；`_resetUi()` 同步清理该类。
  - **css/modules.css**：新增 `.ceremony-hint--center` 样式（`position: fixed; left: 50%; bottom: 18vh; transform: translateX(-50%)`）；`.cream-progress` 新增 `.cream-progress--hidden`（`visibility: hidden; opacity: 0`）替代 `display: none`，使进度条在隐藏时仍占据布局空间，配合 0.35s 渐隐过渡。
  - **js/modules/creamMaking.js**：`_setStageLayout()` 中进度条切换改为 `cream-progress--hidden` 类替代 `.hidden`。
  - **index.html**：`#cream-progress` 初始状态添加 `cream-progress--hidden` 类避免首次渲染闪烁。
- 影响功能：模块7 许愿阶段提示文案更沉浸、位置居中；模块3 打蛋/打发/滴色阶段切换时 UI 不再上下抖动。
- 涉及文件：js/modules/ceremony.js、css/modules.css、js/modules/creamMaking.js、index.html。
- 备注 / 后续：已通过 Playwright 截图逐步验证三阶段（egg → whip → color）布局位置一致，许愿提示居中正确。

### 2026-04-22 — 装饰涂鸦功能全面升级：裱花模组、画画蜡笔效果、滑条选色器

- 背景：需要在装饰涂鸦界面新增裱花模组实现打奶油袋画裱花效果；同时将原有涂鸦模组更名为画画模组并改为蜡笔质感；选色功能需增强滑条选色器。
- 关键变动：
  - **js/config.js**：新增 `creamStampColors` 数组，定义 6 色奶油印章（粉/红/黄/绿/蓝/紫）与图片路径映射；`stateVersion` 升级 2→3 以触发旧状态重置。
  - **index.html**：
    - 模块4「抹奶油 & 涂鸦」→「抹奶油 & 画画」，提示文案改为卡通绘画导向；笔刷大小范围调整为 4-40；新增滑条选色器 HTML 结构。
    - 模块5 Tab 列表新增「裱花」（cream）和「画画」（paint），「裱花」排首位；「蜡烛/食材/插件」保持不变；裱花工具有独立的奶油印章选择器 + 花嘴大小滑块；画画工具有颜色芯片 + 滑条选色器 + 笔刷大小 + 透明度。
  - **js/utils.js**：
    - `drawCrayonStroke()` 全新实现：从原来调用奶油笔触改为三层叠绘（三层透明度/线宽递减的主色描边）+ 沿路径的颗粒纹理噪点 + 微弱白色高光线，呈现蜡笔质感。
    - `drawCreamStampStroke()` 新增：沿笔触路径等间距放置奶油印章图片，模拟挤奶油花纹效果；支持 seeded noise 实现随机旋转和大小扰动。
    - `drawColorSlider()` 新增：彩虹色相条滑块，支持拖拽选色，颜色过渡自然；使用 `beginPath` + `arc` 手动绘制圆角矩形（兼容旧浏览器）。
    - `hslToHex()` 新增：HSL → Hex 颜色转换，用于滑块选色回调。
    - `drawCakeArtwork()` 更新：新增 `creamStrokes` 和 `creamStampImages` 参数；根据 `stroke.type` 区分蜡笔（crayon）和奶油笔触（cream）使用对应绘制函数。
  - **js/modules/decorate.js**：
    - 新增 `creamStrokes` 独立笔触数组，与 `strokes`（画画）分离存储。
    - Tab 逻辑更新：cream 和 paint Tab 显示对应工具栏并隐藏素材轮播；其他 Tab 隐藏工具栏并显示素材轮播。
    - 裱花模式：创建 `type: 'cream'` 笔触，使用 `drawCreamStampStroke` 渲染；画画模式：创建 `type: 'crayon'` 笔触，使用 `drawCrayonStroke` 渲染。
    - 新增 `_setupCreamControls()`（奶油印章选择器 + 花嘴大小初始化）、`_updateTip()`（各 Tab 提示文案）、`_renderCreamStrokes()`（裱花笔触渲染）、`_loadCreamStampImages()`（预加载 6 色印章图片）。
  - **js/modules/creamPaint.js**：将笔触从奶油改为蜡笔（调用 `drawCrayonStroke`）；新增滑条选色器初始化；笔刷大小范围调整为 4-40；更新提示文案为卡通绘画导向。
  - **js/app.js**：`getDefaultState()` 新增 `creamStrokes: []` 和 `editorSettings.creamSize: 24`；`normalizeState()` 新增 `creamStrokes` 归一化处理；`hasDraftCake()` 新增 `creamStrokes` 长度检查。
  - **css/modules.css**：新增 `.decorate-cream-tools`（裱花工具栏）、`.cream-stamp-picker`（印章圆形按钮组）、`.cream-stamp-chip`（选中缩放+棕色边框）、`.color-slider-picker`（滑条容器）、`.color-slider-canvas`（彩虹色相条）、`.color-slider-thumb`（白色圆形滑块指示器）。
  - **js/modules/screenshot.js**：新增 `_creamStampImages` 加载与 `creamStrokes` 渲染支持，使截图中的裱花效果正确呈现。
  - **js/modules/ceremony.js** / **js/modules/cakeCut.js**：同样新增 `_creamStampImages` 加载与 `creamStrokes` 渲染支持，确保仪式和切蛋糕模块中裱花效果正确。
- 影响功能：
  - 新增裱花模组：用户可在装饰界面选择六色奶油印章，在蛋糕上绘制奶油花纹。
  - 画画模组：笔触从奶油质感改为蜡笔质感，适合卡通图案绘制；新增滑条选色器提供连续色彩选择。
  - 所有裱花和画画笔触均会正确保存并在截图、仪式、切蛋糕模块中渲染。
  - 由于 `stateVersion` 升级，旧版未完成蛋糕状态会重置。
- 涉及文件：config.js、index.html、utils.js、decorate.js、creamPaint.js、app.js、modules.css、screenshot.js、ceremony.js、cakeCut.js。
- 备注 / 后续：建议全流程走一遍确认裱花印章颜色切换、花嘴大小调整、蜡笔颗粒感强度是否满意；滑条选色器在触屏设备上的拖动手感可进一步优化。

### 2026-04-22 (2) — 装饰/许愿/奶油制作多模块 bug 修复与体验优化

- 背景：装饰蛋糕界面裱花模组手机端无法显示图案且图案重叠；画画模组滑块不可见、饱和度偏高、缺少明度调节；提示文本内容混淆且与按钮重叠；许愿模块"请许愿"文本偏右未居中；吹蜡烛无语音反馈；首页缺少版本标识；奶油制作进度条导致 UI 抖动；蜡烛交互提示动画异常；切蛋糕盘子图片显示不完整。
- 关键变动：
  - **css/modules.css**：
    1. 裱花印章选择器：`.cream-stamp-chip` 从 44px 圆形改为 60px 圆角矩形，`gap` 从 0.55rem 增至 1rem，`border-radius` 从 50% 改为 `var(--radius-md)`，内部图片 `border-radius` 从 50% 改为 4px，背景透明度提高，解决图案重叠和手机端不可见问题。
    2. 画画提示文本 `.decorate-tip` 新增 `padding-bottom: 2.5rem`，避免与"完成装饰去许愿"按钮重叠。
    3. 许愿文字 `.ceremony-wish-text` 从 `position: absolute` 改为 `position: fixed`，确保在屏幕正中显示而非相对于 `.ceremony-area` 偏移。
    4. 蜡烛提示 `.ceremony-hint` 的 `transition` 从 `all 0.5s ease` 改为 `color 0.3s ease, font-size 0.3s ease`，避免切换提示文案时产生飞行动画。
    5. 奶油进度条 `.cream-progress` 新增 `min-height: 42px`，配合 `cream-progress--hidden` 的 `visibility: hidden` 保留布局空间。
    6. 切蛋糕盘子 `.plate-dropzone` 移除 `transform: translate(10%, 8%)`，调整 `right/bottom` 为 4%，宽度从 `clamp(120px, 28vw, 180px)` 增至 `clamp(130px, 30vw, 190px)`，解决图片被裁切问题。
    7. 新增 `.cover-version` 样式（小号半透明白色文字），用于首页版本号显示。
    8. 新增 `.brightness-slider` 样式，与 `.opacity-slider` 保持一致。
  - **css/main.css**：
    1. `input[type="range"]` 轨道高度从 6px 增至 8px，背景从 `rgba(139,90,43,0.12)` 加深至 `rgba(139,90,43,0.18)`，滑块从 18px 增至 22px，阴影加深，解决手机端滑块不可见问题。
    2. 新增 `::-moz-range-track` 样式，确保 Firefox 下轨道也可见。
  - **js/utils.js**：
    1. `drawColorSlider()` 色相条饱和度从 85% 降至 65%、明度从 60% 升至 72%，实现小清新配色风格。
    2. 新增 `adjustBrightness(hex, brightness)` 工具函数，根据明度系数调整颜色亮度。
  - **js/modules/decorate.js**：
    1. `_setupCreamControls()` 改用 `document.createElement('img')` 代替 `innerHTML`，添加 `loading='eager'` 和 `aria-label`，提升手机端图片加载可靠性。
    2. `_updateTip()` 裱花提示文案改为"选好奶油颜色和花嘴大小，在蛋糕上像拿裱花袋一样挤出花纹吧！"。
    3. `_setupPaintControls()` 新增明度滑块初始化。
    4. `_bindUiEvents()` 新增 `decorate-paint-brightness` 滑块事件绑定。
    5. 画画笔触创建时新增 `brightness` 字段。
    6. `_renderStrokes()` 渲染时根据 `brightness` 值调用 `Utils.adjustBrightness()` 调整颜色。
  - **js/modules/ceremony.js**：
    1. `_onAllBlown()` 新增 `_playBlowOutSound()` 调用，吹灭蜡烛时播放音效。
    2. 新增 `_playBlowOutSound()` 方法，使用 Web Audio API 生成 0.6s 低通滤波噪声模拟吹灭音效，无需外部音频文件。
  - **js/modules/creamMaking.js**：`_setStageLayout()` 进度条切换从 `.hidden`（`display: none`）改为 `.cream-progress--hidden`（`visibility: hidden; opacity: 0`），消除布局抖动。
  - **js/modules/cover.js**：`init()` 新增版本号元素 `#cover-version` 的文本设置为 `v${CONFIG.stateVersion}`。
  - **js/app.js**：`getDefaultState()` 的 `editorSettings` 新增 `brightness: 72` 默认值。
  - **js/config.js**：`stateVersion` 从 3 升级至 4。
  - **index.html**：
    1. 装饰画画工具栏新增明度滑块 `<div class="brightness-slider">`。
    2. 封面新增 `<span class="cover-version" id="cover-version"></span>` 版本号显示。
- 影响功能：
  - 裱花模组：手机端图案正常显示，各奶油印章独立不重叠。
  - 画画模组：滑块在手机端清晰可见，色相条为小清新低饱和风格，新增明度调节控制。
  - 提示文本：裱花/画画提示内容正确，不再与按钮重叠。
  - 许愿模块："请许愿"文本屏幕居中，吹灭蜡烛有音效反馈。
  - 首页：显示当前版本号（如 v4），便于区分缓存版本。
  - 奶油制作：进度条出现/消失不再导致 UI 抖动。
  - 蜡烛提示：切换文案时无异常飞行动画。
  - 切蛋糕：盘子图片完整显示。
- 涉及文件：css/modules.css、css/main.css、js/utils.js、js/modules/decorate.js、js/modules/ceremony.js、js/modules/creamMaking.js、js/modules/cover.js、js/app.js、js/config.js、index.html。
- 备注 / 后续：建议在手机端真机全流程验证，重点关注裱花图案可见性、滑块操作手感、吹灭蜡烛音效、进度条防抖效果；stateVersion 升级至 4 会导致旧版未完成蛋糕状态重置。

### 2026-04-22 (3) — 许愿/裱花/吹蜡烛模块 bug 修复与体验优化

- 背景：许愿提示文本未居中对齐；裱花图案全部挤在一起重叠；吹蜡烛功能交互问题，用户已执行吹气操作但蜡烛未能成功吹灭；缺少麦克风音量检测反馈功能。
- 关键变动：
  - **css/modules.css**：
    1. 许愿文本 `.ceremony-wish-text`：将 `white-space: nowrap` 改为 `white-space: normal`，新增 `text-align: center` 和 `max-width: 80%`，确保文本在各种屏幕尺寸下都能居中显示。
    2. 新增 `.ceremony-vol-meter` 和 `.ceremony-vol-bar` 样式，实现麦克风音量检测反馈的可视化进度条。
  - **js/utils.js**：
    1. `drawCreamStampStroke()` 函数：将 `spacing` 从 `Math.max(stampSize * 0.35, width * 0.42)` 增大到 `Math.max(stampSize * 0.6, width * 0.8)`，并将 `stepCount` 计算从 `Math.floor` 改为 `Math.ceil`，确保裱花图案之间有足够的间距，避免重叠。
  - **js/modules/ceremony.js**：
    1. 初始化时获取音量计元素 `#ceremony-vol-meter` 和 `#ceremony-vol-bar`。
    2. `_resetUi()`：添加音量计重置逻辑。
    3. `_beginBlowPhase()`：添加音量计显示逻辑。
    4. `_startBlowDetection()`：
       - 降低吹气检测阈值：`dynamicScoreThreshold` 从 `Math.max(20, baselineScore * 1.85)` 降至 `Math.max(12, baselineScore * 1.45)`
       - 降低 `dynamicBassThreshold` 从 `Math.max(10, baselineBass * 1.6)` 降至 `Math.max(6, baselineBass * 1.3)`
       - 降低 `burstDetected` 阈值：`peak >= 16 || rms >= 9` 降至 `peak >= 12 || rms >= 6`
       - 降低 `sustainedDetected` 阈值：`blowScore >= Math.max(16, baselineScore * 1.45)` 降至 `blowScore >= Math.max(10, baselineScore * 1.25)`，`rms >= 7` 降至 `rms >= 5`
       - 降低 `blowFrames` 触发阈值从 8 降至 6
       - 新增音量计更新逻辑，实时显示麦克风音量强度
    5. `_onAllBlown()`：添加音量计隐藏逻辑。
  - **index.html**：
    1. 在蜡烛仪式模块中新增音量计元素 `<div class="ceremony-vol-meter hidden" id="ceremony-vol-meter">` 及其内部 `<div class="ceremony-vol-bar" id="ceremony-vol-bar"></div>`。
- 影响功能：
  - 许愿模块："请许愿"文本现在在屏幕中央居中显示，支持自动换行。
  - 裱花模块：奶油印花图案之间有适当的间距，不再重叠挤在一起。
  - 吹蜡烛功能：吹气检测灵敏度提高，用户吹气更容易触发蜡烛熄灭；新增音量计可视化反馈，直观显示吹气强度。
- 涉及文件：css/modules.css、js/utils.js、js/modules/ceremony.js、index.html。
- 备注 / 后续：建议在不同设备上测试吹气检测的灵敏度，确保在各种环境下都能正常触发蜡烛熄灭；音量计的视觉效果可根据需要进一步优化。

### 2026-04-22 (4) — 裱花交互重构：点击生成图章 + 超出边界不裁剪

- 背景：裱花模组原为拖拽画笔模式，用户难以精确控制单个图章位置；裱花图章部分超出蛋糕边界时被蒙版裁剪截断，图案不完整。
- 关键变动：
  - **js/modules/decorate.js**：
    1. `_bindCanvasEvents` `start` 处理器：裱花标签页下，点击画布立即生成单个 `cream-stamp` 类型图章（含 `nx/ny/width/rotation/seed`），不再启动拖拽画笔模式。
    2. `_renderCreamStrokes`：拆分为两种渲染路径——旧路径笔触（`type !== 'cream-stamp'`）仍使用 `renderMaskedLayer` 蒙版裁剪保持兼容；新点击图章（`type === 'cream-stamp'`）直接绘制到画布，不经过蒙版裁剪，确保图章完整显示。
    3. `_updateTip`：裱花提示文案改为"选好奶油花型和大小，点击蛋糕放置裱花图章吧！"。
  - **js/utils.js** `drawCakeArtwork`：
    1. 同步拆分渲染路径：旧路径笔触用 `renderMaskedLayer`，新点击图章直接绘制不裁剪，确保许愿/切蛋糕等场景也正确渲染。
  - **js/config.js**：`stateVersion` 从 5 升级至 6。
- 影响功能：
  - 裱花模组：交互方式从拖拽画笔变为点击放置，每次点击生成一个完整图章，位置精确可控。
  - 图章边缘：部分超出蛋糕边界的图章不再被截断，完整显示整个图案。
  - 向后兼容：旧版拖拽生成的路径笔触仍可正常渲染。
- 涉及文件：js/modules/decorate.js、js/utils.js、js/config.js。
- 备注 / 后续：stateVersion 升级至 6 会导致旧版未完成蛋糕状态重置；图章大小可通过花嘴大小滑块调节。